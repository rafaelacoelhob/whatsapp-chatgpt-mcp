# WhatsApp -> ChatGPT (projeto Ana Savoia)
# Sobe 3 containers: banco (Postgres), Evolution API (ponte com o WhatsApp)
# e o servidor MCP (conector que o ChatGPT acessa).
#
# Uso: copiar .env.example para .env, preencher, e rodar: docker compose up -d

services:
  postgres:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_USER: evolution
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: evolution
    volumes:
      - evolution_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U evolution"]
      interval: 10s
      timeout: 5s
      retries: 5

  evolution:
    # se a imagem não for encontrada, alternativa: atendai/evolution-api:v2.2.3
    image: evoapicloud/evolution-api:latest
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8080:8080" # painel/manager e API — pode ficar só na rede interna
    environment:
      SERVER_PORT: "8080"
      AUTHENTICATION_API_KEY: ${EVOLUTION_API_KEY}
      DATABASE_ENABLED: "true"
      DATABASE_PROVIDER: postgresql
      DATABASE_CONNECTION_URI: postgresql://evolution:${POSTGRES_PASSWORD}@postgres:5432/evolution
      DATABASE_SAVE_DATA_INSTANCE: "true"
      DATABASE_SAVE_DATA_NEW_MESSAGE: "true"
      DATABASE_SAVE_DATA_CONTACTS: "true"
      DATABASE_SAVE_DATA_CHATS: "true"
      CACHE_LOCAL_ENABLED: "true"
      CACHE_REDIS_ENABLED: "false"
      DEL_INSTANCE: "false"
      QRCODE_LIMIT: "30"
      LANGUAGE: pt-BR
    volumes:
      - evolution_instances:/evolution/instances

  mcp:
    build: ./mcp-server
    restart: always
    depends_on:
      - evolution
    ports:
      - "8090:8080" # PRECISA ser exposto em HTTPS público (ex: wamcp.livemode.com) p/ o ChatGPT acessar
    environment:
      PORT: "8080"
      EVOLUTION_URL: http://evolution:8080
      EVOLUTION_API_KEY: ${EVOLUTION_API_KEY}
      EVOLUTION_INSTANCE: ${EVOLUTION_INSTANCE:-ana}
      MCP_PATH_SECRET: ${MCP_PATH_SECRET}
      BLOCKED_NUMBERS: ${BLOCKED_NUMBERS:-}

volumes:
  evolution_pgdata:
  evolution_instances:
